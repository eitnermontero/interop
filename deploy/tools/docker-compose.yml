# TOOLS stack (Consul, Redis, Vault, Keycloak) - isolated from the apps.
#
# Always-on: keycloak (no profile).
# Toggle single <-> cluster via COMPOSE_PROFILES in .env:
#   COMPOSE_PROFILES=single   -> 1 redis + 1 consul + 1 vault (fast dev)
#   COMPOSE_PROFILES=cluster  -> 3 consul + 6 redis + 3 vault (raft HA, prod-like)
#
# Binds to 0.0.0.0 (HUB_TOOLS_BIND_IP) to be reachable from other Docker stacks
# (apps hub-gateway, hub-base-service, etc.) and from the LAN network (browser).
name: ${HUB_TOOLS_STACK_NAME:-hub-tools}

# External shared network - must exist BEFORE bringing up the stack.
# Create it once with:
#   docker network create --driver bridge --opt com.docker.network.driver.mtu=1500 hub-shared
# Override the name with HUB_SHARED_NETWORK in .env.
networks:
  hub_tools_net:
    name: ${HUB_TOOLS_NETWORK_NAME:-hub-tools-net}
    driver: bridge
  hub_shared:
    name: ${HUB_SHARED_NETWORK:-hub-shared}
    external: true

volumes:
  hub_keycloak_data:
  # Single mode
  hub_consul_data:
  hub_redis_data:
  hub_vault_data:
  # Cluster mode
  hub_consul_s1_data:
  hub_consul_s2_data:
  hub_consul_s3_data:
  hub_redis_m1_data:
  hub_redis_m2_data:
  hub_redis_m3_data:
  hub_redis_r1_data:
  hub_redis_r2_data:
  hub_redis_r3_data:
  hub_vault_1_data:
  hub_vault_2_data:
  hub_vault_3_data:

# === Centralized images - change each component version in ONE place here ===
# (overrideable via env: HUB_REDIS_IMAGE / HUB_CONSUL_IMAGE / HUB_VAULT_IMAGE / KEYCLOAK_IMAGE)
x-keycloak-image: &keycloak-image "${KEYCLOAK_IMAGE:-quay.io/keycloak/keycloak:26.6}"
x-consul-image:   &consul-image   "${HUB_CONSUL_IMAGE:-hashicorp/consul:1.22}"
x-redis-image:    &redis-image    "${HUB_REDIS_IMAGE:-redis:8.8}"
x-vault-image:    &vault-image    "${HUB_VAULT_IMAGE:-hashicorp/vault:1.21}"

# === Shared resource limits via YAML anchors =================================
x-consul-deploy: &consul-deploy
  resources:
    limits:
      memory: ${HUB_CONSUL_MEM_LIMIT:-256M}
      cpus: "${HUB_CONSUL_CPU_LIMIT:-0.5}"
    reservations:
      memory: ${HUB_CONSUL_MEM_RESERVATION:-128M}
      cpus: "${HUB_CONSUL_CPU_RESERVATION:-0.1}"

x-redis-deploy: &redis-deploy
  resources:
    limits:
      memory: ${HUB_REDIS_MEM_LIMIT:-256M}
      cpus: "${HUB_REDIS_CPU_LIMIT:-0.5}"
    reservations:
      memory: ${HUB_REDIS_MEM_RESERVATION:-64M}
      cpus: "${HUB_REDIS_CPU_RESERVATION:-0.1}"

x-vault-deploy: &vault-deploy
  resources:
    limits:
      memory: ${HUB_VAULT_MEM_LIMIT:-256M}
      cpus: "${HUB_VAULT_CPU_LIMIT:-0.5}"
    reservations:
      memory: ${HUB_VAULT_MEM_RESERVATION:-64M}
      cpus: "${HUB_VAULT_CPU_RESERVATION:-0.1}"

x-redis-healthcheck: &redis-healthcheck
  test: ["CMD-SHELL", "redis-cli -p 6379 ping | grep -q PONG"]
  interval: ${HUB_HC_REDIS_INTERVAL:-10s}
  timeout: ${HUB_HC_REDIS_TIMEOUT:-5s}
  retries: ${HUB_HC_REDIS_RETRIES:-12}

x-consul-healthcheck: &consul-healthcheck
  test: ["CMD", "consul", "members"]
  interval: ${HUB_HC_CONSUL_INTERVAL:-10s}
  timeout: ${HUB_HC_CONSUL_TIMEOUT:-5s}
  retries: ${HUB_HC_CONSUL_RETRIES:-12}

# Vault: exit 0 = unsealed/active, 2 = sealed/standby (both = "alive"). 1 = down.
x-vault-healthcheck: &vault-healthcheck
  test: ["CMD-SHELL", "vault status > /dev/null 2>&1 || [ $$? -eq 2 ]"]
  interval: ${HUB_HC_VAULT_INTERVAL:-10s}
  timeout: ${HUB_HC_VAULT_TIMEOUT:-5s}
  retries: ${HUB_HC_VAULT_RETRIES:-12}

services:

  # ===========================================================================
  # ALWAYS-ON (no profile)
  # ===========================================================================

  keycloak:
    container_name: ${HUB_KEYCLOAK_NAME:-hub-keycloak}
    image: *keycloak-image
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ${HUB_KEYCLOAK_MEM_LIMIT:-768M}
          cpus: "${HUB_KEYCLOAK_CPU_LIMIT:-1.0}"
        reservations:
          memory: ${HUB_KEYCLOAK_MEM_RESERVATION:-384M}
          cpus: "${HUB_KEYCLOAK_CPU_RESERVATION:-0.25}"
    command: "${KEYCLOAK_COMMAND:-start-dev}"
    environment:
      # Storage backend: 'dev-file' (dev, in-memory, pierde datos al reiniciar),
      # 'postgres' (producción, requiere KC_DB_URL + KC_DB_USERNAME + KC_DB_PASSWORD).
      KC_DB: ${KEYCLOAK_DB:-dev-file}
      # KC_DB_URL/USERNAME/PASSWORD: solo setear cuando KC_DB=postgres (staging/prod).
      # Pasar string vacío rompe el datasource H2 de dev-file en Keycloak 26.
      # Descomentar y completar para PostgreSQL backend:
      # KC_DB_URL: ${KEYCLOAK_DB_URL}
      # KC_DB_USERNAME: ${KEYCLOAK_DB_USERNAME}
      # KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      # Keycloak 26+: usar KC_BOOTSTRAP_ADMIN_* (los legacy KEYCLOAK_ADMIN* están deprecados y no funcionan en 26.6)
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USER:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_FEATURES: ${KEYCLOAK_FEATURES:-scripts}
      KC_HTTP_PORT: ${KEYCLOAK_INTERNAL_HTTP_PORT:-9080}
      KC_HTTPS_PORT: ${KEYCLOAK_INTERNAL_HTTPS_PORT:-9443}
      KC_HEALTH_ENABLED: ${KEYCLOAK_HEALTH_ENABLED:-true}
      KC_HTTP_MANAGEMENT_PORT: ${KEYCLOAK_MANAGEMENT_PORT:-9990}
      # Producción: hostname público que Keycloak embebe en el JWT iss y en los redirects.
      # Debe coincidir con HUB_KEYCLOAK_URL en los servicios.
      KC_HOSTNAME: ${KEYCLOAK_HOSTNAME:-}
      KC_HOSTNAME_STRICT: ${KEYCLOAK_HOSTNAME_STRICT:-false}
      # Proxy headers — solo setear si hay reverse proxy (nginx/traefik) delante.
      # Valores válidos: forwarded | xforwarded. Omitir cuando no hay proxy (Keycloak 26 rechaza string vacío).
      # KC_PROXY_HEADERS: ${KEYCLOAK_PROXY_HEADERS}   # descomentar si KEYCLOAK_PROXY_HEADERS está definido en .env
      # HTTP enabled incluso con HTTPS configurado (para reverse proxy que termina TLS).
      KC_HTTP_ENABLED: ${KEYCLOAK_HTTP_ENABLED:-true}
      # X.509 Certificate Lookup: leer cert del partner desde header X-SSL-Client-Cert
      # inyectado por nginx (mTLS termination en reverse proxy).
      # Permite que Keycloak emita JWT con cnf.x5t#S256 (RFC 8705).
      KC_SPI_X509CERT_LOOKUP_PROVIDER: ${KEYCLOAK_X509_LOOKUP_PROVIDER:-nginx}
      KC_SPI_X509CERT_LOOKUP_NGINX_SSL_CLIENT_CERT: ${KEYCLOAK_X509_HEADER:-X-SSL-Client-Cert}
      KC_SPI_X509CERT_LOOKUP_NGINX_CERTIFICATE_CHAIN_LENGTH: "0"
    networks:
      - hub_tools_net
      - hub_shared
    ports:
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${KEYCLOAK_HTTP_PORT:-8180}:${KEYCLOAK_INTERNAL_HTTP_PORT:-9080}"
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${KEYCLOAK_HTTPS_PORT:-8443}:${KEYCLOAK_INTERNAL_HTTPS_PORT:-9443}"
    volumes:
      - ./keycloak/health-check.sh:/opt/keycloak/health-check.sh:ro
      - hub_keycloak_data:/opt/keycloak/data
    healthcheck:
      test: ["CMD-SHELL", "bash /opt/keycloak/health-check.sh"]
      interval: 5s
      timeout: 5s
      retries: 50
      start_period: 10s
    labels:
      org.springframework.boot.ignore: true

  # ===========================================================================
  # SINGLE MODE (profile "single") - default
  # ===========================================================================

  consul:
    profiles: ["single"]
    container_name: ${HUB_CONSUL_NAME:-hub-consul}
    image: *consul-image
    restart: unless-stopped
    deploy: *consul-deploy
    command: ["agent", "-dev", "-ui", "-client=0.0.0.0", "-log-level=${HUB_CONSUL_LOG_LEVEL:-INFO}"]
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_consul_data:/consul/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${HUB_CONSUL_HTTP_PORT:-8500}:8500"
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${HUB_CONSUL_RPC_PORT:-8300}:8300"
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${HUB_CONSUL_DNS_PORT:-8600}:8600/udp"
    healthcheck: *consul-healthcheck

  redis:
    profiles: ["single"]
    container_name: ${HUB_REDIS_NAME:-hub-redis}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command: ["redis-server", "--appendonly", "${HUB_REDIS_APPENDONLY:-yes}", "--notify-keyspace-events", "Ex"]
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${HUB_REDIS_PORT:-6379}:6379"
    healthcheck: *redis-healthcheck

  # Vault single - dev mode: unsealed + root token = HUB_VAULT_DEV_ROOT_TOKEN.
  # DO NOT use in prod. Data is lost on restart (in-memory storage).
  vault:
    profiles: ["single"]
    container_name: ${HUB_VAULT_NAME:-hub-vault}
    image: *vault-image
    restart: unless-stopped
    deploy: *vault-deploy
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: ${HUB_VAULT_DEV_ROOT_TOKEN:-root}
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
      VAULT_ADDR: http://127.0.0.1:8200
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_vault_data:/vault/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-127.0.0.1}:${HUB_VAULT_PORT:-8200}:8200"
    healthcheck: *vault-healthcheck

  # ===========================================================================
  # CLUSTER MODE (profile "cluster") - prod-like HA, failover, slot rebalance
  # ===========================================================================
  # Consul: 3 servers, bootstrap-expect=3 (waits for quorum), retry-join between all.
  # Redis:  3 masters + 3 replicas. cluster-announce-ip = $HUB_HOST_IP (REQUIRED
  #         in cluster) so external clients can reach nodes via MOVED. Uses
  #         ${HUB_HOST_IP:-} (not ':?') to avoid parse errors in single mode, where
  #         these services are not created; in cluster mode redis/vault-cluster-init.sh validates it.
  # Vault:  3 nodes with integrated raft storage + auto-join. After up requires init + unseal:
  #           scripts/vault-cluster-init.sh
  # After `up -d` with cluster mode, run ONCE:
  #   scripts/redis-cluster-init.sh
  #   scripts/vault-cluster-init.sh

  consul-server-1:
    profiles: ["cluster"]
    container_name: ${HUB_CONSUL_S1_NAME:-hub-consul-server-1}
    image: *consul-image
    restart: unless-stopped
    deploy: *consul-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec consul agent \
          -server -ui -client=0.0.0.0 \
          -bind='{{ GetInterfaceIP "eth0" }}' \
          -node=${HUB_CONSUL_S1_NAME:-hub-consul-server-1} \
          -datacenter=${HUB_CONSUL_DATACENTER:-dc-hub-prod} \
          -bootstrap-expect=3 \
          -data-dir=/consul/data \
          -log-level=${HUB_CONSUL_LOG_LEVEL:-INFO} \
          -retry-join=${HUB_CONSUL_S2_NAME:-hub-consul-server-2} \
          -retry-join=${HUB_CONSUL_S3_NAME:-hub-consul-server-3}
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_consul_s1_data:/consul/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_CONSUL_S1_HTTP_PORT:-8500}:8500"
    healthcheck: *consul-healthcheck

  consul-server-2:
    profiles: ["cluster"]
    container_name: ${HUB_CONSUL_S2_NAME:-hub-consul-server-2}
    image: *consul-image
    restart: unless-stopped
    deploy: *consul-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec consul agent \
          -server -ui -client=0.0.0.0 \
          -bind='{{ GetInterfaceIP "eth0" }}' \
          -node=${HUB_CONSUL_S2_NAME:-hub-consul-server-2} \
          -datacenter=${HUB_CONSUL_DATACENTER:-dc-hub-prod} \
          -bootstrap-expect=3 \
          -data-dir=/consul/data \
          -log-level=${HUB_CONSUL_LOG_LEVEL:-INFO} \
          -retry-join=${HUB_CONSUL_S1_NAME:-hub-consul-server-1} \
          -retry-join=${HUB_CONSUL_S3_NAME:-hub-consul-server-3}
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_consul_s2_data:/consul/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_CONSUL_S2_HTTP_PORT:-8501}:8500"
    healthcheck: *consul-healthcheck

  consul-server-3:
    profiles: ["cluster"]
    container_name: ${HUB_CONSUL_S3_NAME:-hub-consul-server-3}
    image: *consul-image
    restart: unless-stopped
    deploy: *consul-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec consul agent \
          -server -ui -client=0.0.0.0 \
          -bind='{{ GetInterfaceIP "eth0" }}' \
          -node=${HUB_CONSUL_S3_NAME:-hub-consul-server-3} \
          -datacenter=${HUB_CONSUL_DATACENTER:-dc-hub-prod} \
          -bootstrap-expect=3 \
          -data-dir=/consul/data \
          -log-level=${HUB_CONSUL_LOG_LEVEL:-INFO} \
          -retry-join=${HUB_CONSUL_S1_NAME:-hub-consul-server-1} \
          -retry-join=${HUB_CONSUL_S2_NAME:-hub-consul-server-2}
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_consul_s3_data:/consul/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_CONSUL_S3_HTTP_PORT:-8502}:8500"
    healthcheck: *consul-healthcheck

  redis-master-1:
    profiles: ["cluster"]
    container_name: ${HUB_REDIS_M1_NAME:-hub-redis-master-1}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec redis-server --bind 0.0.0.0 --port 6379 \
          --cluster-enabled yes --cluster-config-file /data/nodes.conf \
          --cluster-node-timeout ${HUB_REDIS_CLUSTER_NODE_TIMEOUT_MS:-10000} \
          --cluster-announce-ip ${HUB_HOST_IP:-} \
          --cluster-announce-port ${HUB_REDIS_M1_PORT:-6379} \
          --cluster-announce-bus-port ${HUB_REDIS_M1_BUS_PORT:-16379} \
          --appendonly yes --protected-mode no
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_m1_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_M1_PORT:-6379}:6379"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_M1_BUS_PORT:-16379}:16379"
    healthcheck: *redis-healthcheck

  redis-master-2:
    profiles: ["cluster"]
    container_name: ${HUB_REDIS_M2_NAME:-hub-redis-master-2}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec redis-server --bind 0.0.0.0 --port 6379 \
          --cluster-enabled yes --cluster-config-file /data/nodes.conf \
          --cluster-node-timeout ${HUB_REDIS_CLUSTER_NODE_TIMEOUT_MS:-10000} \
          --cluster-announce-ip ${HUB_HOST_IP:-} \
          --cluster-announce-port ${HUB_REDIS_M2_PORT:-6380} \
          --cluster-announce-bus-port ${HUB_REDIS_M2_BUS_PORT:-16380} \
          --appendonly yes --protected-mode no
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_m2_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_M2_PORT:-6380}:6379"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_M2_BUS_PORT:-16380}:16379"
    healthcheck: *redis-healthcheck

  redis-master-3:
    profiles: ["cluster"]
    container_name: ${HUB_REDIS_M3_NAME:-hub-redis-master-3}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec redis-server --bind 0.0.0.0 --port 6379 \
          --cluster-enabled yes --cluster-config-file /data/nodes.conf \
          --cluster-node-timeout ${HUB_REDIS_CLUSTER_NODE_TIMEOUT_MS:-10000} \
          --cluster-announce-ip ${HUB_HOST_IP:-} \
          --cluster-announce-port ${HUB_REDIS_M3_PORT:-6381} \
          --cluster-announce-bus-port ${HUB_REDIS_M3_BUS_PORT:-16381} \
          --appendonly yes --protected-mode no
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_m3_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_M3_PORT:-6381}:6379"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_M3_BUS_PORT:-16381}:16379"
    healthcheck: *redis-healthcheck

  redis-replica-1:
    profiles: ["cluster"]
    container_name: ${HUB_REDIS_R1_NAME:-hub-redis-replica-1}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec redis-server --bind 0.0.0.0 --port 6379 \
          --cluster-enabled yes --cluster-config-file /data/nodes.conf \
          --cluster-node-timeout ${HUB_REDIS_CLUSTER_NODE_TIMEOUT_MS:-10000} \
          --cluster-announce-ip ${HUB_HOST_IP:-} \
          --cluster-announce-port ${HUB_REDIS_R1_PORT:-6382} \
          --cluster-announce-bus-port ${HUB_REDIS_R1_BUS_PORT:-16382} \
          --appendonly yes --protected-mode no
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_r1_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_R1_PORT:-6382}:6379"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_R1_BUS_PORT:-16382}:16379"
    healthcheck: *redis-healthcheck

  redis-replica-2:
    profiles: ["cluster"]
    container_name: ${HUB_REDIS_R2_NAME:-hub-redis-replica-2}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec redis-server --bind 0.0.0.0 --port 6379 \
          --cluster-enabled yes --cluster-config-file /data/nodes.conf \
          --cluster-node-timeout ${HUB_REDIS_CLUSTER_NODE_TIMEOUT_MS:-10000} \
          --cluster-announce-ip ${HUB_HOST_IP:-} \
          --cluster-announce-port ${HUB_REDIS_R2_PORT:-6383} \
          --cluster-announce-bus-port ${HUB_REDIS_R2_BUS_PORT:-16383} \
          --appendonly yes --protected-mode no
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_r2_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_R2_PORT:-6383}:6379"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_R2_BUS_PORT:-16383}:16379"
    healthcheck: *redis-healthcheck

  redis-replica-3:
    profiles: ["cluster"]
    container_name: ${HUB_REDIS_R3_NAME:-hub-redis-replica-3}
    image: *redis-image
    restart: unless-stopped
    deploy: *redis-deploy
    command:
      - /bin/sh
      - -ec
      - |
        exec redis-server --bind 0.0.0.0 --port 6379 \
          --cluster-enabled yes --cluster-config-file /data/nodes.conf \
          --cluster-node-timeout ${HUB_REDIS_CLUSTER_NODE_TIMEOUT_MS:-10000} \
          --cluster-announce-ip ${HUB_HOST_IP:-} \
          --cluster-announce-port ${HUB_REDIS_R3_PORT:-6384} \
          --cluster-announce-bus-port ${HUB_REDIS_R3_BUS_PORT:-16384} \
          --appendonly yes --protected-mode no
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_redis_r3_data:/data
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_R3_PORT:-6384}:6379"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_REDIS_R3_BUS_PORT:-16384}:16379"
    healthcheck: *redis-healthcheck

  # Vault cluster - 3 nodes with integrated raft storage + auto-join.
  # After up requires init + unseal: scripts/vault-cluster-init.sh
  # api_addr/cluster_addr use HUB_HOST_IP so the rest of the cluster
  # and external clients can reach each node.

  vault-1:
    profiles: ["cluster"]
    container_name: ${HUB_VAULT_1_NAME:-hub-vault-1}
    image: *vault-image
    restart: unless-stopped
    deploy: *vault-deploy
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_ADDR: http://127.0.0.1:8200
      VAULT_API_ADDR: http://${HUB_HOST_IP:-}:${HUB_VAULT_1_PORT:-8200}
      # cluster_addr uses internal DNS between containers (raft peers) - the host LAN
      # IP is not reachable from a container to the same host by default.
      VAULT_CLUSTER_ADDR: http://${HUB_VAULT_1_NAME:-hub-vault-1}:8201
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        chown -R vault:vault /vault/data 2>/dev/null || true
        exec docker-entrypoint.sh vault server -config=/vault/config/vault.hcl
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_vault_1_data:/vault/data
      - ./vault/vault-1.hcl:/vault/config/vault.hcl:ro
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_VAULT_1_PORT:-8200}:8200"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_VAULT_1_CLUSTER_PORT:-8201}:8201"
    healthcheck: *vault-healthcheck

  vault-2:
    profiles: ["cluster"]
    container_name: ${HUB_VAULT_2_NAME:-hub-vault-2}
    image: *vault-image
    restart: unless-stopped
    deploy: *vault-deploy
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_ADDR: http://127.0.0.1:8200
      VAULT_API_ADDR: http://${HUB_HOST_IP:-}:${HUB_VAULT_2_PORT:-8210}
      VAULT_CLUSTER_ADDR: http://${HUB_VAULT_2_NAME:-hub-vault-2}:8201
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        chown -R vault:vault /vault/data 2>/dev/null || true
        exec docker-entrypoint.sh vault server -config=/vault/config/vault.hcl
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_vault_2_data:/vault/data
      - ./vault/vault-2.hcl:/vault/config/vault.hcl:ro
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_VAULT_2_PORT:-8210}:8200"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_VAULT_2_CLUSTER_PORT:-8211}:8201"
    healthcheck: *vault-healthcheck

  vault-3:
    profiles: ["cluster"]
    container_name: ${HUB_VAULT_3_NAME:-hub-vault-3}
    image: *vault-image
    restart: unless-stopped
    deploy: *vault-deploy
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_ADDR: http://127.0.0.1:8200
      VAULT_API_ADDR: http://${HUB_HOST_IP:-}:${HUB_VAULT_3_PORT:-8220}
      VAULT_CLUSTER_ADDR: http://${HUB_VAULT_3_NAME:-hub-vault-3}:8201
    entrypoint: ["/bin/sh", "-ec"]
    command:
      - |
        chown -R vault:vault /vault/data 2>/dev/null || true
        exec docker-entrypoint.sh vault server -config=/vault/config/vault.hcl
    networks:
      - hub_tools_net
      - hub_shared
    volumes:
      - hub_vault_3_data:/vault/data
      - ./vault/vault-3.hcl:/vault/config/vault.hcl:ro
    ports:
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_VAULT_3_PORT:-8220}:8200"
      - "${HUB_TOOLS_BIND_IP:-0.0.0.0}:${HUB_VAULT_3_CLUSTER_PORT:-8221}:8201"
    healthcheck: *vault-healthcheck
