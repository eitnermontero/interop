services:
  hub-admin-service:
    image: ${HUB_ADMIN_IMAGE:-cr.sintesis.com.bo/hub-dev/hub-admin-service}:${HUB_ADMIN_VERSION:-latest}
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ${HUB_ADMIN_MEM_LIMIT:-1024M}
        reservations:
          memory: ${HUB_ADMIN_MEM_RESERVATION:-512M}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${LOG_DIR:-~/logs/hub}${TENANT_ID:+-}${TENANT_ID}/admin-service/:/app/logs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      TZ: America/La_Paz
      TENANT_ID: ${TENANT_ID:-}

      # --- JVM ---
      _JAVA_OPTIONS: ${HUB_ADMIN_JAVA_OPTS:-}

      # --- Spring profile ---
      SPRING_PROFILES_ACTIVE: ${HUB_ADMIN_PROFILE:-dev}

      # --- Server ---
      SERVER_PORT: 8083

      # --- Service discovery (Consul) ---
      SPRING_CLOUD_CONSUL_HOST: ${HUB_CONSUL_HOST:-host.docker.internal}
      SPRING_CLOUD_CONSUL_PORT: ${HUB_CONSUL_PORT:-8500}
      SPRING_CLOUD_CONSUL_SCHEME: ${HUB_CONSUL_SCHEME:-http}
      SPRING_CLOUD_CONSUL_TOKEN: ${HUB_CONSUL_TOKEN:-}
      # Service-name en Consul SIN guiones (convencion del sistema; igual que base-service/gateway).
      SPRING_CLOUD_CONSUL_DISCOVERY_SERVICE_NAME: hubadminservice${TENANT_ID:+-}${TENANT_ID}
      SPRING_CLOUD_CONSUL_CONFIG_ENABLED: ${HUB_CONSUL_CONFIG_ENABLED:-false}
      # Consul external (otra Docker network) no alcanza el IP interno del container, asi que
      # desactivamos el HTTP active check. La service queda registrada en el catalog sin health-check.
      SPRING_CLOUD_CONSUL_DISCOVERY_REGISTER_HEALTH_CHECK: "false"

      # --- Vault ---
      SPRING_CLOUD_VAULT_HOST: ${HUB_VAULT_HOST:-host.docker.internal}
      SPRING_CLOUD_VAULT_PORT: ${HUB_VAULT_PORT:-8200}
      SPRING_CLOUD_VAULT_SCHEME: ${HUB_VAULT_SCHEME:-http}
      SPRING_CLOUD_VAULT_AUTHENTICATION: ${HUB_VAULT_AUTHENTICATION:-TOKEN}
      SPRING_CLOUD_VAULT_TOKEN: ${HUB_VAULT_TOKEN:-root}
      SPRING_CLOUD_VAULT_APP_ROLE_ROLE_ID: ${HUB_VAULT_ROLE_ID:-}
      SPRING_CLOUD_VAULT_APP_ROLE_SECRET_ID: ${HUB_VAULT_SECRET_ID:-}
      # Namespace tenant-aware: secret/hub-auth[-<tenant>]/...
      SPRING_CLOUD_VAULT_KV_DEFAULT_CONTEXT: ${HUB_AUTH_VAULT_NS:-hub-auth}${TENANT_ID:+-}${TENANT_ID}
      SPRING_CLOUD_VAULT_KV_APPLICATION_NAME: ${HUB_AUTH_VAULT_NS:-hub-auth}${TENANT_ID:+-}${TENANT_ID}
      # KV mount: 'secret' (dev) o 'kv' (stage, vault cluster monta kv/).
      SPRING_CLOUD_VAULT_KV_BACKEND: ${HUB_VAULT_KV_BACKEND:-secret}
      # Namespace tenant-aware para los config.import de Vault (ver application.yml).
      HUB_VAULT_NS: ${HUB_AUTH_VAULT_NS:-hub-auth}${TENANT_ID:+-}${TENANT_ID}
      # Mount KV de Vault para los config.import (dev=secret, stage=kv).
      HUB_VAULT_MOUNT: ${HUB_VAULT_MOUNT:-secret}
      # VAULT_NS y VAULT_MOUNT son leídos directamente por ${VAULT_NS:...} en application.yml
      VAULT_NS: ${HUB_AUTH_VAULT_NS:-hub-auth}${TENANT_ID:+-}${TENANT_ID}
      VAULT_MOUNT: ${HUB_VAULT_MOUNT:-secret}

      # --- Database (PostgreSQL) — schema 'admin' ---
      SPRING_DATASOURCE_URL: ${HUB_ADMIN_DB_URL:-jdbc:postgresql://host.docker.internal:5432/hub_auth}
      SYSTEM_DATABASE_USERNAME: ${HUB_ADMIN_DB_USERNAME:-hub}
      SYSTEM_DATABASE_PASSWORD: ${HUB_ADMIN_DB_PASSWORD:-}

      # --- Redis (RedisTemplate, aislamiento multi-tenant por prefijo de key) ---
      SYSTEM_REDIS_HOST: ${HUB_REDIS_HOST:-host.docker.internal}
      SYSTEM_REDIS_PORT: ${HUB_REDIS_PORT:-6379}
      # password (clave) -> Vault secret/<ns>/system/redis. NO en env.
      # Spring Data cluster: NO se setea aca (lista vacia rompe Lettuce). En stage se
      # activa con el perfil 'redis-cluster' (SPRING_PROFILES_ACTIVE) + HUB_REDIS_CLUSTER_NODES.

      # --- Keycloak / OIDC ---
      KEYCLOAK_SERVICE_CLIENT_AUTH_SERVER_URL: ${HUB_KEYCLOAK_URL:-http://host.docker.internal:8180}
      KEYCLOAK_SERVICE_CLIENT_REALM: ${HUB_KEYCLOAK_REALM_BASE:-hub-admin}${TENANT_ID:+-}${TENANT_ID}
      APPLICATION_KEYCLOAK_EXTERNAL_URL: ${HUB_KEYCLOAK_EXTERNAL_URL:-${HUB_KEYCLOAK_URL:-http://host.docker.internal:8180}}
      APPLICATION_KEYCLOAK_REALM: ${HUB_KEYCLOAK_REALM_BASE:-hub-admin}${TENANT_ID:+-}${TENANT_ID}
      # Service account para invocar la Keycloak Admin API (gestion de users/roles + poll de eventos LOGIN/LOGOUT).
      KEYCLOAK_ADMIN_CLIENT_AUTH_SERVER_URL: ${HUB_KEYCLOAK_URL:-http://host.docker.internal:8180}
      KEYCLOAK_ADMIN_CLIENT_REALM: ${HUB_KEYCLOAK_REALM_BASE:-hub-admin}${TENANT_ID:+-}${TENANT_ID}
      # client-id, client-secret, grant-type -> Vault keycloak/admin-client (100% de Vault, igual
      # que service-client). Tenerlos en env interferia con la carga del client-secret de Vault.
      # Shared secret for the Keycloak HTTP Event Listener webhook (X-Keycloak-Secret header).
      # Must match AUDIT_KC_SECRET seeded in Vault (audit/keycloak-secret) and configured
      # in the Keycloak realm event listener when the keycloak-http-event-listener SPI provider
      # is deployed. Override here only if not using Vault.
      APPLICATION_AUDIT_KEYCLOAK_SECRET: ${HUB_ADMIN_AUDIT_KC_SECRET:-}

      # --- Logging ---
      # LOG_DIR + LOG_FILE los lee logback-spring.xml para el RollingFileAppender.
      LOG_DIR: /app/logs
      LOG_FILE: admin.log
      LOGGING_LEVEL_ROOT: ${HUB_ADMIN_ROOT_LOG_LEVEL:-INFO}
      LOGGING_LEVEL_BO_COM_SINTESIS_HUB_AUTH: ${HUB_ADMIN_LOG_LEVEL:-INFO}
      LOGGING_LOGBACK_ROLLINGPOLICY_MAX_FILE_SIZE: ${HUB_LOG_MAX_FILE_SIZE:-500MB}
      LOGGING_LOGBACK_ROLLINGPOLICY_MAX_HISTORY: ${HUB_LOG_MAX_HISTORY:-90}

    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:8083/management/health | grep -q '\"status\":\"UP\"'"]
      interval: ${HUB_HC_API_INTERVAL:-30s}
      timeout: ${HUB_HC_API_TIMEOUT:-10s}
      retries: ${HUB_HC_API_RETRIES:-3}
      start_period: ${HUB_HC_API_START_PERIOD:-60s}
